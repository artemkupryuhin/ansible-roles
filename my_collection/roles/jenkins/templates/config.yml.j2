---
app:
  port: 18080
  host: 0.0.0.0
redis:
  port: 6379
  host: "127.0.0.1"
  pass: "{{ jenkins.pass }}"
kafka: "{% for node in groups[ kafka_cluster_group_hosts ] %}{{ node }}:9092{% if not loop.last %},{% endif %}{%endfor%}"